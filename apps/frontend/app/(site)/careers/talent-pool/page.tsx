import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Talent Pool',
    description: 'Join the Ocean Ceylon Holdings talent pool for future opportunities.',
};

export { default } from '@/views/careers/TalentPool';
