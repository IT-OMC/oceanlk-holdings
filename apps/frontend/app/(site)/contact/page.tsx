import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Contact Us',
    description: 'Get in touch with Ocean Ceylon Holdings.',
};

export { default } from '@/views/Contact';
