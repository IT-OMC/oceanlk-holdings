spring.application.name=oceanlk-backend
server.port=8080

# MongoDB Configuration
# Using local MongoDB for development (reliable and tested)
spring.data.mongodb.uri=mongodb://localhost:27017/oceanlk
# Note: MongoDB Atlas requires Java SSL certificate configuration - see documentation for production setup
# spring.data.mongodb.uri=mongodb+srv://dm_db_user:PsHZ1frsPkkTGhgF@cluster0.xe161cj.mongodb.net/oceanlk?retryWrites=true&w=majority

# Logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# Email Configuration (SMTP)
# Note: Configure these values with your actual SMTP credentials
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email From Address
app.email.from=noreply@oceanlk.com
app.email.hr=hr@omc.lk

# JWT Configuration
app.jwt.secret=oceanlk_super_secret_jwt_key_change_this_in_production_2026
app.jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
