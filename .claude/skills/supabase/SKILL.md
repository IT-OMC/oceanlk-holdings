---
name: supabase
description: "Use when doing ANY task involving Supabase. Triggers: Supabase products (Database, Auth, Edge Functions, Realtime, Storage, Vectors, Cron, Queues); client libraries and SSR integrations (supabase-js, @supabase/ssr) in Next.js; auth issues (login, logout, sessions, JWT, cookies, getSession, getUser, getClaims, RLS); Supabase CLI or MCP server; schema changes, migrations, security audits, Postgres extensions (pg_graphql, pg_cron, pg_vector)."
---

Canonical source: `.agents/skills/supabase/SKILL.md` (installed/tracked via `skills-lock.json`
— don't hand-edit the original, and don't hand-edit this pointer's content either). This file
exists only so Claude Code (which loads project skills from `.claude/skills/`) picks it up.
Its `references/` and `assets/` subfolders live alongside the canonical file, e.g.
`.agents/skills/supabase/references/skill-feedback.md`.

@../../../../.agents/skills/supabase/SKILL.md
